name: Check

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install rust-toolchain
        uses: actions-rs/toolchain@v1.0.6
        with:
          toolchain: stable

      - name: Install go
        uses: actions/setup-go@v5
        with:
          go-version: "stable"
      
      - name: Install .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install LLVM and Clang
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 18
          sudo ln -sfT clang++-18 /usr/bin/clang++
          sudo ln -sfT clang-18 /usr/bin/clang
        shell: bash

      - name: Install Boost
        run: |
          sudo apt install libboost-all-dev
      
      - name: Setup common environment
        run: |
          export CC=/usr/bin/clang
          export CXX=/usr/bin/clang++
          echo "CC=/usr/bin/clang" >> $GITHUB_ENV
          echo "CXX=/usr/bin/clang++" >> $GITHUB_ENV

      - name: Build - Bitcoin Core
        timeout-minutes: 40
        run: |
          export BOOST_LIB_DIR=/usr/lib/x86_64-linux-gnu/
          cd modules/bitcoin && make

      - name: Build - rust modules
        timeout-minutes: 5
        run: |
          rustup default nightly
          cd modules/rust && make cargo && make

      - name: Build - btcd
        timeout-minutes: 5
        run: |
          cd modules/btcd && make
      
      - name: Build - lnd
        timeout-minutes: 5
        run: |
          cd modules/lnd && make

      - name: Build - nlightning
        timeout-minutes: 5
        run: |
          cd modules/nlightning && make

      - name: Build - bitcoinfuzz
        timeout-minutes: 5
        run: |
          export CXXFLAGS="-DBITCOIN_CORE -DRUST_MODULES -DBTCD -DNLIGHTNING"
          make

      - name: Test - script
        run: FUZZ=script ./bitcoinfuzz -runs=100
      
      - name: Test - block_deserialization
        run: FUZZ=block_deserialization ./bitcoinfuzz -runs=100
      
      - name: Test - script_eval
        run: FUZZ=script_eval ./bitcoinfuzz -runs=100
      
      - name: Test - descriptor_parse
        run: FUZZ=descriptor_parse ./bitcoinfuzz -runs=100
      
      - name: Test - miniscript_parse
        run: FUZZ=miniscript_parse ./bitcoinfuzz -runs=100
      
      - name: Test - script_asm
        run: FUZZ=script_asm ./bitcoinfuzz -runs=100
      
      - name: Test - invoice_deserialization
        run: FUZZ=invoice_deserialization ./bitcoinfuzz -runs=100
